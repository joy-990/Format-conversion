# Format-conversion
A reusable skill for inspecting multimodal dataset schemas, understanding dataset intent from samples and documentation, and converting image, audio, document, and mixed-modality data into a canonical training JSONL format.
